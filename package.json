{
  "name": "apitomarkdown",
  "version": "1.0.0",
  "description": "A tool for converting data obtained from an API to a set of markdown files for use in a static site generator",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "start": "node dist/index.js"
  },
  "keywords": [
    "api",
    "markdown",
    "static",
    "site",
    "generator"
  ],
  "author": "David Dasenbrook",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/node": "^10.3.4",
    "@types/request": "^2.47.1"
  },
  "dependencies": {
    "request": "^2.87.0"
  }
}
